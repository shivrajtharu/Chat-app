# Project setup

1. Initialize npm
   - npm init -y

2. Install Dependencies
   - npm install
   - npm install bcryptjs cors dotenv express helmet jsonwebtoken mongoose nodemon socket.io

3. setup the mongodb Atlas

4. command to run project
   - npm run dev
